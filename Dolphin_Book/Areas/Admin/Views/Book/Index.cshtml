@model List<Book>
<div class="card-body">
    <table class="table">
        <thead>
            <tr class="text-nowrap">
                <th>Id</th>
                <th>Image</th>
                <th>Name</th>
                <th>Count</th>
                <th>Purchase Price</th>
                <th>Sale Price</th>
                <th>Created Time</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <th scope="row">@item.Id</th>
                    <td><img src="~/admin/img/images/@item.Image" width="100px" style="object-fit: contain;" height="100px"></td>
                    <td>@item.Name</td>
                    <td>@item.Count</td>
                    <td>@item.PurchasePrice</td>
                    <td>@item.SalePrice</td>
                    <td>@item.CreatedAt</td>

                    <td><a asp-controller="book" asp-action="update" asp-route-id="@item.Id" class="fa-solid fa-pen-to-square"><i></i></a></td>
                    <td><a asp-controller="book" asp-action="delete" asp-route-id="@item.Id" class="fa-solid fa-trash-can"><i></i></a></td>

                </tr>
            }

        </tbody>
    </table>
    <a asp-area="admin" asp-controller="book" asp-action="create"><button type="button" class="btn btn-primary btn-lg btn-block"> Create Book </button></a>

</div>